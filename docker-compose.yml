version: "3.9"

services:
  agent:
    build: ./agent
    ports:
      - "8000:8000"
    volumes:
      - ./data/logs:/logs
    environment:
      - LOG_DIR=/logs

  dashboard:
    build: ./dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./data/logs:/logs
